# Frontend (Vite + React + Tailwind)

Quick notes:

- Run locally after installing deps:

```bash
npm install
npm run dev
```

- Environment variables: copy `.env.example` to `.env`.
- Frontend dev server uses Vite and runs on http://localhost:5173 by default.
